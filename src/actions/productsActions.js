import { PRODUCTS_FETCH,
    PRODUCTS_FETCH_SUCCESS,
    PRODUCTS_FETCH_FAIL } from "./types";

const getAllProducts = (page = 1) => (dispatch, getState) => {

}

const sortProducts = (sortType) => (dispatch, getState) => {

}

const filterProductsByBrand = (brand = 'All') => (dispatch, getState) => {

}

const filterProductsByTagName = () => (dispatch, getState) => {
    
}