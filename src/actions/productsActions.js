import { PRODUCTS_FETCH,
    PRODUCTS_FETCH_SUCCESS,
    PRODUCTS_FETCH_FAIL,FILTER_PRODUCTS_BY_BRAND,
    FILTER_PRODUCTS_BY_TAG,
    SORT_PRODUCTS } from "./types";

const getAllProducts = (page = 1) => (dispatch, getState) => {

}

const sortProducts = (sortType = 'LOW_TO_HIGH') => (dispatch, getState) => {

}

const filterProductsByBrand = (brands = []) => (dispatch, getState) => {

}

const filterProductsByTagName = (tags = []) => (dispatch, getState) => {
    
}
