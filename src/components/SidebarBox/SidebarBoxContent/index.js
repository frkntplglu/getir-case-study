import styled from "styled-components";

const SidebarBoxContent = styled.div`
  background-color: #fff;
  border-radius: ${(props) => props.theme.commonRadius};
  padding: 24px;
`;

export default SidebarBoxContent;
