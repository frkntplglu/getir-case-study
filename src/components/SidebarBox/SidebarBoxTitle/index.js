import styled from "styled-components";

const SidebarBoxTitle = styled.div`
  font-size: 13px;
  margin-bottom: 12px;
  font-weight: 600;
  color: ${(props) => props.theme.colors.titleColor};
`;

export default SidebarBoxTitle;
