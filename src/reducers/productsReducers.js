import { PRODUCTS_FETCH,
    PRODUCTS_FETCH_SUCCESS,
    PRODUCTS_FETCH_FAIL } from "../actions/types";

const productsReducers = (state = {}, action) => {
    return state
}

export {productsReducers}